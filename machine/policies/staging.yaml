environment: staging
logging:
  level: info
security:
  require_https: true
  allow_localhost: false
runtime:
  reload: false
  workers: 2
backup:
  enabled: true
  retention_days: 7
monitoring:
  enabled: true
agent:
  mode: managed
ai:
  provider_mode: hybrid
